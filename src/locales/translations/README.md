# Adding a New Language

1. Fork this repository
2. Copy `src/locales/en.json` to `src/locales/{locale}.json` (e.g., `fr.json` for French)
3. Translate all values (keep the keys in English)
4. Submit a Pull Request

The card auto-detects the language from your Home Assistant instance settings.
